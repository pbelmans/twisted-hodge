import setuptools

setuptools.setup(
        name="twisted",
        version="1",
        description="Compute twisted Hodge numbers of complete intersections",
        url="https://github.com/pbelmans/twisted-hodge",
        packages=setuptools.find_packages(),
        zip_safe=False,
)
